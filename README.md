# SPECT_DL_denoise

SPECT simulation → OSEM reconstruction → Deep learning denoising (3D U-Net).

## Structure
- `src/`: reusable python modules (simulation/noise/recon)
- `notebooks/`: demo notebooks (reproduce pipeline, training)
- `scripts/`: HPC/Myriad job scripts
- `outputs/`: generated outputs (ignored by git)
